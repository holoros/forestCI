% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crown-width.R
\name{max_crown_width}
\alias{max_crown_width}
\alias{largest_crown_width}
\title{Maximum and largest crown width}
\usage{
max_crown_width(dbh, species, traits = species_traits())

largest_crown_width(dbh, species, traits = species_traits(), mcw = NULL)
}
\arguments{
\item{dbh}{Numeric vector of diameter at breast height, cm.}

\item{species}{Character vector of species codes, recycled against `dbh`.}

\item{traits}{Trait table, normally from [species_traits()].}

\item{mcw}{Maximum crown width, m. Supplied to `largest_crown_width()` to
avoid recomputing it.}
}
\value{
Numeric vector of crown widths, m.
}
\description{
`max_crown_width()` returns maximum crown width, the crown width an
open-grown tree of that diameter would carry, and is the quantity crown
competition factor is built from. `largest_crown_width()` returns the
widest crown width of the tree as it actually stands, which is the radius
the crown profile is scaled to.
}
\details{
Both use the power forms of the Acadian variant,
MCW = a1 * DBH^a2 and LCW = MCW / (b1 * DBH^b2), with MCW and LCW in m and
DBH in cm. Coefficients come from the species trait table, so a species with
no local coefficients falls back to the softwood or hardwood default rather
than failing.
}
\examples{
max_crown_width(c(10, 25, 40), c("RS", "RS", "SM"))
}
\references{
Russell, M.B., Weiskittel, A.R. (2011) Maximum and largest crown width
equations for 15 tree species in Maine. Northern Journal of Applied
Forestry 28: 84-91.
}
