% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/competition.R
\name{competition_indices}
\alias{competition_indices}
\title{Compute competition indices}
\usage{
competition_indices(
  stand,
  indices = c("independent", "dependent", "apa", "rapa", "crown", "pattern"),
  profile = crown_profile(),
  radius = 6,
  filter = "none",
  edge = c("flag", "exclude", "none"),
  edge_method = "exterior_angle",
  edge_buffer = radius,
  edge_angle = 120,
  apa_weight = "dbh",
  apa_exponent = 1,
  rapa_exponent = 1,
  rapa_resolution = 0.25,
  crown_osv = TRUE,
  n_depth = 18L,
  n_azimuth = 72L,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{stand}{A `stand` from [as_stand()].}

\item{indices}{Character vector of index families to compute, any of
`"independent"`, `"dependent"`, `"apa"`, `"rapa"`, `"crown"`,
`"pattern"`. Default all.}

\item{profile}{A `crown_profile` for the crown based indices.}

\item{radius}{Search radius, m, for the pairwise indices. Default 6.}

\item{filter}{Competitor filter passed to [neighbors()].}

\item{edge}{Edge handling. `"flag"` adds the `is_edge` column and leaves the
values in place. `"exclude"` additionally sets every distance-dependent
value to `NA` for edge trees, which is the conservative choice and the one
Kuehne et al. (2019) made. `"none"` skips the edge computation.}

\item{edge_method, edge_buffer, edge_angle}{Passed to [edge_flag()].}

\item{apa_weight, apa_exponent}{Weighting variable and exponent for [apa()].}

\item{rapa_exponent}{Exponent for [rapa()]. Kept separate because the
rasterised construction is multiplicatively weighted, so an exponent that
is mild in [apa()] is aggressive here. Default 1.}

\item{rapa_resolution}{Cell size for [rapa()], m.}

\item{crown_osv}{Compute open sky view, the expensive crown index.}

\item{n_depth, n_azimuth}{Crown facet grid, passed to [crown_exposure()].}

\item{quiet}{Suppress the message listing skipped families.}

\item{...}{Passed to [neighbors()].}
}
\value{
A `data.table` with one row per tree, carrying the stand columns and
  every computed index.
}
\description{
The single entry point. Given a stand, it computes every index the data
will support and returns one row per tree. Distance-independent indices are
always computed. Distance-dependent indices are computed when stem
coordinates are present, crown based indices when height and height to
crown base are present, and the rest are returned as `NA` columns with one
message saying which were skipped and why, rather than silently omitted.
}
\examples{
st <- pef_stand()
ci <- competition_indices(st, indices = c("independent", "dependent"))
str(ci)
}
